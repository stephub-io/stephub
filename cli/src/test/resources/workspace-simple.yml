name: "abc2"
#providers:
#  - name: rest-api
#    version: "> 1.0"
#    baseUrl: http://localhost:8080


stepDefinitions:
  - type: basic
    spec:
      pattern: My step
    steps:
      - Given I do something
      - Then I expect something

features:
  - name: Feature 1
    scenarios:
      - type: inline
        name: Scene 1
        steps:
          - Given I do something
          - Then I expect something